export const getPrivateProfile = (email: string, jwt_token: string) => {
    
} 


export const getPublicProfile = (user_id: string) => {
    
    
}

export const changeProfileFirstName = (user_id: string, email: string, jwt_token: string) => {
    
}

export const changeProfileLastName = (user_id: string, email: string, jwt_token: string) => {
    
}

export const changeProfileAvatar = (user_id: string, email: string, jwt_token: string) => {
    
}

export const deleteProfileAvatar = (user_id: string, email: string, jwt_token: string) => {
    
}

export const changeStatus = (user_id: string, email: string, jwt_token: string, status: string) => {
    
}

export const changeBio = (user_id: string, email: string, jwt_token: string, bio: string) => {
    
}

export const changePassword = (user_id: string, email: string, jwt_token: string, currentPassword: string, newPassword: string) => {
    
}

export const closeAccount = (user_id: string, email: string, jwt_token: string, message: string) => {
    
}